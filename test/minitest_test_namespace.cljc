; (ns minitest-test-namespace
;   (:require [minitest :include-macros true])) ;; TODO: rework

; ;; Keep this form the second in the file
; (minitest/tests
;   (inc 1) := 2
;   (inc 3) := 4
;   (inc 3) := 5
;   nil     := nil
;   nil     := true)
